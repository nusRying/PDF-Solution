---
phase: 08-advanced-semantic-structures
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: [src/pdf_accessibility/skills/remediation/tables.py, src/pdf_accessibility/skills/remediation/forms.py, src/pdf_accessibility/skills/registry.py]
autonomous: true
requirements: [TBL-01, TBL-02, FRM-02]

must_haves:
  truths:
    - "Blocks are correctly re-assigned roles (table_header, table_data) within a table."
    - "Form fields with missing tooltips receive auto-generated tooltips from names."
    - "Remediation skills correctly transform the canonical document's structure."
  artifacts:
    - path: "src/pdf_accessibility/skills/remediation/tables.py"
      provides: "TableRepairSkill"
    - path: "src/pdf_accessibility/skills/remediation/forms.py"
      provides: "FormRepairSkill"
  key_links:
    - from: "src/pdf_accessibility/skills/remediation/tables.py"
      to: "src/pdf_accessibility/services/tables.py"
      via: "Dependency on detection service for grid analysis"
---

<objective>
Implement remediation skills that transform the raw canonical document by grouping blocks into semantic table structures and enhancing form field accessibility metadata.

Purpose: Bridge the gap between detection and standards-compliant structural tagging.
Output: Two new remediation skills and registration in the SkillRegistry.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pdf_accessibility/skills/base.py
@src/pdf_accessibility/skills/registry.py
@.planning/phases/08-advanced-semantic-structures/08-01-SUMMARY.md

<interfaces>
From src/pdf_accessibility/skills/base.py:
```python
class BaseSkill(ABC):
    @abstractmethod
    def execute(self, document: CanonicalDocument) -> RemediationArtifact:
        pass
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement TableRepairSkill (D-01)</name>
  <files>src/pdf_accessibility/skills/remediation/tables.py, tests/test_table_repair.py</files>
  <behavior>
    - Scan pages for `CanonicalTable` structures.
    - Re-assign roles of constituent `CanonicalBlock`s to `table_header` or `table_data`.
    - Handle association of `caption` blocks if detected near the table.
    - Generate `RemediationArtifact` detailing changes.
  </behavior>
  <action>
    - Create `TableRepairSkill` inheriting from `BaseSkill`.
    - Use `TableDetectionService` if tables aren't already present in the canonical document.
    - Transform document structure: move blocks into `CanonicalTable` hierarchy and update roles.
  </action>
  <verify>
    <automated>pytest tests/test_table_repair.py</automated>
  </verify>
  <done>TableRepairSkill correctly restructures canonical document with table roles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FormRepairSkill (D-02)</name>
  <files>src/pdf_accessibility/skills/remediation/forms.py, tests/test_form_repair.py</files>
  <behavior>
    - Identify form fields with empty tooltips (`TU` is missing).
    - Propose remediation: Set tooltip to match field name or a human-readable version of it.
    - Ensure every interactive element has an accessible name.
  </behavior>
  <action>
    - Create `FormRepairSkill` inheriting from `BaseSkill`.
    - Scan `CanonicalForm` list.
    - Update tooltip metadata where missing.
  </action>
  <verify>
    <automated>pytest tests/test_form_repair.py</automated>
  </verify>
  <done>FormRepairSkill ensures all form fields have valid tooltips.</done>
</task>

<task type="auto">
  <name>Task 3: Register Skills in SkillRegistry</name>
  <files>src/pdf_accessibility/skills/registry.py</files>
  <action>
    - Import `TableRepairSkill` and `FormRepairSkill`.
    - Add to `SkillRegistry` initialization in the `remediation` category.
  </action>
  <verify>
    <automated>python -c "from pdf_accessibility.skills.registry import SkillRegistry; reg = SkillRegistry(); print('table_repair' in reg.list_skills('remediation'))"</automated>
  </verify>
  <done>Skills are registered and available for pipeline execution.</done>
</task>

</tasks>

<verification>
- Skills correctly modify `CanonicalDocument` roles.
- `RemediationArtifact` contains clear audit logs for table and form changes.
- Registry recognizes new skills.
</verification>

<success_criteria>
- Unit tests for both skills pass.
- Integration tests confirm skills are executed as part of the remediation pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/08-advanced-semantic-structures/08-02-SUMMARY.md`
</output>
