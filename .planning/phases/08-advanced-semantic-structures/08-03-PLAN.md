---
phase: 08-advanced-semantic-structures
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: [src/pdf_accessibility/services/tagging.py]
autonomous: true
requirements: [TAG-01, TAG-02]

must_haves:
  truths:
    - "TaggingEngine produces nested `/Table` -> `/TR` -> `/TH`/`/TD` structure."
    - "Form fields are correctly tagged as `/Form` with MCIDs."
    - "E2E remediation pipeline produces PDF/UA-1 compliant tables and forms."
  artifacts:
    - path: "src/pdf_accessibility/services/tagging.py"
      provides: "Recursive tagging logic"
  key_links:
    - from: "src/pdf_accessibility/services/tagging.py"
      to: "pikepdf"
      via: "StructElem hierarchy generation"
---

<objective>
Update the `TaggingEngine` to handle recursive semantic structures, specifically nested table tagging and interactive form field tagging, and verify the entire Milestone 8 feature set with E2E tests.

Purpose: Generate fully compliant structural tagging for advanced PDF elements.
Output: Enhanced `TaggingEngine` and comprehensive validation tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pdf_accessibility/services/tagging.py
@.planning/phases/08-advanced-semantic-structures/08-02-SUMMARY.md

<interfaces>
From src/pdf_accessibility/services/tagging.py:
```python
ROLE_TO_TAG_MAP = {
    # ...
    CanonicalRole.table: "/Table",
    # ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TaggingEngine for Nested Tables (D-03)</name>
  <files>src/pdf_accessibility/services/tagging.py</files>
  <action>
    - Add roles to `ROLE_TO_TAG_MAP`: `table_row: "/TR"`, `table_header: "/TH"`, `table_data: "/TD"`, `form_field: "/Form"`.
    - Refactor `build_struct_tree` to handle `CanonicalTable` structures recursively.
    - If a block is within a table: create `/TR` -> `/TH` or `/TD` elements appropriately.
    - Attach table attributes like `RowSpan` and `ColSpan` to `/TH`/`/TD` elements if they exceed 1.
    - Ensure `Caption` is handled as part of the `/Table` element if present.
  </action>
  <verify>
    <automated>python -c "from pdf_accessibility.services.tagging import ROLE_TO_TAG_MAP; assert '/TR' in ROLE_TO_TAG_MAP.values()"</automated>
  </verify>
  <done>TaggingEngine correctly generates nested table structures.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Form Field Tagging (D-03)</name>
  <files>src/pdf_accessibility/services/tagging.py</files>
  <action>
    - Ensure `form_field` elements are created in the structure tree.
    - For `/Form` elements, associate the Widget annotation with the structure element (via `/OBJ` or `/K` array including object references).
    - Map form tooltips to `/Alt` text or ensure they are present in the PDF metadata.
  </action>
  <verify>
    <automated>python -c "from pdf_accessibility.services.tagging import ROLE_TO_TAG_MAP; assert '/Form' in ROLE_TO_TAG_MAP.values()"</automated>
  </verify>
  <done>Interactive form fields are correctly represented in the structure tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: E2E Verification & Test Coverage</name>
  <files>tests/test_milestone_8_e2e.py</files>
  <behavior>
    - Use a sample PDF with a simple table and some form fields.
    - Run the full remediation pipeline.
    - Verify the output PDF structure tree using pikepdf.
    - Check for Presence of `/TR`, `/TH`, `/TD` and `/Form` tags.
    - Verify `RowSpan`/`ColSpan` attributes are present.
  </behavior>
  <action>
    - Create a comprehensive E2E test.
    - Assert specific structural requirements (Matterhorn Protocol checks for tables).
  </action>
  <verify>
    <automated>pytest tests/test_milestone_8_e2e.py</automated>
  </verify>
  <done>Milestone 8 features are fully verified and passing in the revival environment.</done>
</task>

</tasks>

<verification>
- Tables are correctly tagged with TR/TH/TD.
- Forms have valid /Form structure elements.
- E2E tests pass for table/form heavy documents.
</verification>

<success_criteria>
- Tagging engine handles nested structures without data loss.
- Output PDFs pass automated accessibility checks for tables and forms.
</success_criteria>

<output>
After completion, create `.planning/phases/08-advanced-semantic-structures/08-03-SUMMARY.md`
</output>
