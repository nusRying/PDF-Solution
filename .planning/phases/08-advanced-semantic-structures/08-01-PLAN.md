---
phase: 08-advanced-semantic-structures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/pdf_accessibility/models/canonical.py, src/pdf_accessibility/services/tables.py, src/pdf_accessibility/services/forms.py]
autonomous: true
requirements: [TBL-01, TBL-02, TBL-03, FRM-01, FRM-02]

must_haves:
  truths:
    - "Table cells are correctly grouped into rows and columns in the model."
    - "Header cells (TH) are distinguished from data cells (TD) in the extracted grid."
    - "AcroForm fields (Name, Tooltip) are correctly extracted from the PDF."
  artifacts:
    - path: "src/pdf_accessibility/services/tables.py"
      provides: "TableDetectionService"
    - path: "src/pdf_accessibility/services/forms.py"
      provides: "FormDetectionService"
    - path: "src/pdf_accessibility/models/canonical.py"
      contains: ["CanonicalTable", "CanonicalRow", "CanonicalCell", "CanonicalForm"]
  key_links:
    - from: "src/pdf_accessibility/services/tables.py"
      to: "src/pdf_accessibility/models/canonical.py"
      via: "Transformation of layout cues to CanonicalTable structure"
---

<objective>
Define the core data structures for advanced semantic elements (Tables & Forms) and implement their initial extraction services using heuristic and PDF metadata analysis.

Purpose: Provide the foundational data models and extraction capabilities required for advanced accessibility remediation.
Output: Updated canonical models and new detection services for tables and forms.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pdf_accessibility/models/canonical.py
@src/pdf_accessibility/services/preflight.py

<interfaces>
From src/pdf_accessibility/models/canonical.py:
```python
class CanonicalRole(str, Enum):
    heading1 = "heading1"
    # ... existing roles ...
    table = "table"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Canonical Models (D-01, D-02)</name>
  <files>src/pdf_accessibility/models/canonical.py</files>
  <action>
    - Add new roles to `CanonicalRole`: `table_row`, `table_header`, `table_data`, `form_field`.
    - Define `CanonicalCell` model: includes `bbox`, `role` (header/data), `rowspan`, `colspan`, and `block_ids: list[str]`.
    - Define `CanonicalRow` model: includes `cells: list[CanonicalCell]`.
    - Define `CanonicalTable` model: includes `table_id`, `rows: list[CanonicalRow]`, `caption: str | None`.
    - Define `CanonicalForm` model: includes `field_id`, `name`, `tooltip`, `bbox`, `role`.
    - Update `CanonicalPage` to include `tables: list[CanonicalTable]` and `forms: list[CanonicalForm]`.
  </action>
  <verify>
    <automated>python -c "from pdf_accessibility.models.canonical import CanonicalTable, CanonicalForm; print('Models loaded')"</automated>
  </verify>
  <done>Canonical models updated and verifiable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TableDetectionService (D-01)</name>
  <files>src/pdf_accessibility/services/tables.py, tests/test_table_detection.py</files>
  <behavior>
    - Identify grid-based layouts by analyzing bounding box alignments (X and Y coordinates).
    - Group blocks into cells based on proximity and grid alignment.
    - Infer header cells (TH) using formatting cues (font-weight: bold, font-size, background shading if available via PyMuPDF).
    - Detect colspans and rowspans by identifying cells that cross row/column boundaries.
  </behavior>
  <action>
    - Create `TableDetectionService` class with `detect_tables(page: CanonicalPage) -> list[CanonicalTable]`.
    - Use PyMuPDF's `get_text("dict")` or `get_drawings()` if needed to identify visual table borders.
    - Implement a grid-fitting algorithm to resolve cell coordinates into logical (R, C) indices.
  </action>
  <verify>
    <automated>pytest tests/test_table_detection.py</automated>
  </verify>
  <done>TableDetectionService correctly identifies grid structure and headers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement FormDetectionService (D-02)</name>
  <files>src/pdf_accessibility/services/forms.py, tests/test_form_detection.py</files>
  <behavior>
    - Extract AcroForm fields from PDF using pikepdf.
    - For each field, capture `T` (Name), `TU` (Tooltip), and its appearance bounding box.
    - Map fields to `CanonicalForm` structure.
  </behavior>
  <action>
    - Create `FormDetectionService` class with `extract_forms(pdf_path: str) -> list[CanonicalForm]`.
    - Map extracted fields back to pages using `/P` reference or intersection of Rect and page dimensions.
  </action>
  <verify>
    <automated>pytest tests/test_form_detection.py</automated>
  </verify>
  <done>FormDetectionService extracts form metadata accurately.</done>
</task>

</tasks>

<verification>
- Canonical models support Table and Form hierarchies.
- TableDetectionService identifies headers and grid layouts.
- FormDetectionService extracts AcroForm metadata.
</verification>

<success_criteria>
- Unit tests pass for both services.
- Models correctly represent nested table and form structures.
</success_criteria>

<output>
After completion, create `.planning/phases/08-advanced-semantic-structures/08-01-SUMMARY.md`
</output>
