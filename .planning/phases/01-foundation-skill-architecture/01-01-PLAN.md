---
phase: 01-foundation-skill-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/pdf_accessibility/skills/base.py, src/pdf_accessibility/skills/registry.py, src/pdf_accessibility/core/metrics.py, tests/test_skill_registry.py]
autonomous: true
requirements: [SKILL-01, OPS-03]

must_haves:
  truths:
    - "Skills can be registered with a unique ID and retrieved later."
    - "Base classes define a standard interface for remediation and validation skills."
    - "Execution metrics for skills are tracked via Prometheus counters/histograms."
  artifacts:
    - path: "src/pdf_accessibility/skills/base.py"
      provides: "Abstract base classes for all processing skills"
    - path: "src/pdf_accessibility/skills/registry.py"
      provides: "Central management of skill lifecycle and lookup"
    - path: "src/pdf_accessibility/core/metrics.py"
      provides: "Prometheus metrics definitions for skills and pipeline"
  key_links:
    - from: "src/pdf_accessibility/skills/registry.py"
      to: "src/pdf_accessibility/skills/base.py"
      via: "Type hints and interface validation"
---

<objective>
Establish the core Skill Architecture including base classes, a centralized registry, and the observability baseline for skill execution.

Purpose: Provide the foundational infrastructure required for rule-driven document processing and multi-standard compliance mapping.
Output: Working Skill Registry and Prometheus-backed observability for skill execution.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@src/pdf_accessibility/core/logging.py
@src/pdf_accessibility/models/remediation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Rule/Skill Base Classes</name>
  <files>src/pdf_accessibility/skills/base.py</files>
  <action>
    - Create abstract base classes for skills using `abc.ABC` and `abc.abstractmethod`.
    - `BaseSkill`: Contains `id`, `name`, `version`, `description`.
    - `RemediationSkill(BaseSkill)`: Adds `remediate(canonical_doc: CanonicalDocument, settings: Settings) -> list[RemediationAction]`.
    - `ValidationSkill(BaseSkill)`: Adds `validate(canonical_doc: CanonicalDocument, settings: Settings) -> list[ValidationFinding]`.
    - Ensure standard imports for `CanonicalDocument`, `Settings`, `RemediationAction`, etc. are present.
  </action>
  <verify>
    <automated>python -c "from pdf_accessibility.skills.base import BaseSkill, RemediationSkill; print('Import successful')"</automated>
  </verify>
  <done>Abstract base classes for skills are defined with clear processing interfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Skill Registry with Tests</name>
  <files>src/pdf_accessibility/skills/registry.py, tests/test_skill_registry.py</files>
  <action>
    - Implement `SkillRegistry` as a class-based singleton or a module-level registry.
    - Provide `register_skill(skill: BaseSkill)` and `get_skill(skill_id: str) -> BaseSkill | None`.
    - Provide `get_skills_by_ids(skill_ids: list[str]) -> list[BaseSkill]`.
    - Create `tests/test_skill_registry.py` verifying:
        - Successful registration and retrieval.
        - Retrieval of multiple skills by list of IDs.
        - Behavior when skill ID is not found (returns None/empty list as appropriate).
  </action>
  <verify>
    <automated>pytest tests/test_skill_registry.py</automated>
  </verify>
  <done>Skill Registry is functional and verified with comprehensive unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Observability Baseline (Prometheus Metrics)</name>
  <files>src/pdf_accessibility/core/metrics.py</files>
  <action>
    - Initialize `prometheus_client` metrics for the platform.
    - Define `SKILL_EXECUTION_TOTAL`: Counter with labels `['skill_id', 'outcome']`.
    - Define `SKILL_EXECUTION_DURATION_SECONDS`: Histogram with labels `['skill_id']`.
    - Provide a helper function `record_skill_execution(skill_id: str, duration: float, success: bool)` to simplify pipeline integration.
  </action>
  <verify>
    <automated>python -c "from pdf_accessibility.core.metrics import SKILL_EXECUTION_TOTAL; print(SKILL_EXECUTION_TOTAL._name)"</automated>
  </verify>
  <done>Prometheus metrics for skill execution are defined and ready for integration.</done>
</task>

</tasks>

<verification>
- Registry unit tests pass.
- Metrics are correctly initialized and accessible.
- Skill base classes allow for correct subclassing (as verified by stub tests in Task 2).
</verification>

<success_criteria>
- All Skill Registry tests pass (`pytest tests/test_skill_registry.py`).
- No regressions in existing health checks.
- Prometheus metrics for skills are visible to the application core.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-skill-architecture/01-01-SUMMARY.md`
</output>
