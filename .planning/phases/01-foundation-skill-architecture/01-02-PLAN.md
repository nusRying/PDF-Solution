---
phase: 01-foundation-skill-architecture
plan: 02
type: execute
wave: 2
depends_on: ["01-01-PLAN.md"]
files_modified: [src/pdf_accessibility/models/compliance.py, src/pdf_accessibility/skills/remediation/text_normalization.py, src/pdf_accessibility/skills/remediation/ocr_confidence.py, src/pdf_accessibility/skills/registry.py]
autonomous: true
requirements: [SKILL-02, SKILL-01]

must_haves:
  truths:
    - "Procedural rules have been encapsulated into Skill classes."
    - "ComplianceProfiles can explicitly list which skill IDs are active."
    - "Skills can be resolved by ID from the global registry."
  artifacts:
    - path: "src/pdf_accessibility/skills/remediation/text_normalization.py"
      provides: "Skill for normalizing text whitespace and nulls (TEXT-001)"
    - path: "src/pdf_accessibility/skills/remediation/ocr_confidence.py"
      provides: "Skill for flagging low-confidence OCR blocks (OCR-010)"
    - path: "src/pdf_accessibility/models/compliance.py"
      provides: "Updated ProfileDefinition including active_skills mapping"
  key_links:
    - from: "src/pdf_accessibility/models/compliance.py"
      to: "src/pdf_accessibility/skills/registry.py"
      via: "Skill ID references in profile definitions"
---

<objective>
Migrate the existing procedural remediation logic into the new Skill architecture and update the ComplianceProfile model to explicitly map to these skills.

Purpose: Decouple remediation/validation logic from the pipeline orchestrator and allow for dynamic profile-based rule selection.
Output: Registered skill classes for Text Normalization and OCR Confidence, and updated ComplianceProfile definitions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation-skill-architecture/01-01-SUMMARY.md
@src/pdf_accessibility/skills/base.py
@src/pdf_accessibility/skills/registry.py
@src/pdf_accessibility/services/remediation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Procedural Rules to Skill Classes</name>
  <files>src/pdf_accessibility/skills/remediation/text_normalization.py, src/pdf_accessibility/skills/remediation/ocr_confidence.py</files>
  <action>
    - Create `TextNormalizationSkill` class in `text_normalization.py`:
        - `id = "TEXT-001"`
        - Implements `remediate()` logic from `_normalize_text` in `services/remediation.py`.
    - Create `OCRConfidenceSkill` class in `ocr_confidence.py`:
        - `id = "OCR-010"`
        - Implements logic from `services/remediation.py` that flags low-confidence OCR blocks.
    - Ensure these classes inherit from `RemediationSkill`.
  </action>
  <verify>
    <automated>python -c "from pdf_accessibility.skills.remediation.text_normalization import TextNormalizationSkill; s = TextNormalizationSkill(); print(s.id)"</automated>
  </verify>
  <done>Text normalization and OCR confidence rules are encapsulated as Skills.</done>
</task>

<task type="auto">
  <name>Task 2: Register Skills and Update Registry Resolver</name>
  <files>src/pdf_accessibility/skills/registry.py</files>
  <action>
    - Import and register `TextNormalizationSkill` and `OCRConfidenceSkill` in the `SkillRegistry`.
    - Implement `resolve_skills_for_profile(profile_definition: ProfileDefinition) -> list[BaseSkill]` in the registry module.
    - This helper should combine `active_remediation_skills` and `active_validation_skills` and return instances from the registry.
  </action>
  <verify>
    <automated>python -c "from pdf_accessibility.skills.registry import SkillRegistry; print(SkillRegistry().get_skill('TEXT-001').id)"</automated>
  </verify>
  <done>Initial skills are registered and accessible via profile-aware resolution logic.</done>
</task>

<task type="auto">
  <name>Task 3: Update ComplianceProfile and ProfileDefinition Models</name>
  <files>src/pdf_accessibility/models/compliance.py</files>
  <action>
    - Update `ProfileDefinition` Pydantic model:
        - Add `active_remediation_skills: list[str] = Field(default_factory=list)`
        - Add `active_validation_skills: list[str] = Field(default_factory=list)`
    - Update `PROFILE_DEFINITIONS`:
        - For all 3 profiles, add `"TEXT-001"` and `"OCR-010"` to `active_remediation_skills`.
  </action>
  <verify>
    <automated>python -c "from pdf_accessibility.models.compliance import get_profile_definition, ComplianceProfile; print(get_profile_definition(ComplianceProfile.profile_a).active_remediation_skills)"</automated>
  </verify>
  <done>Compliance profiles are updated to explicitly map to the first production skills.</done>
</task>

</tasks>

<verification>
- Skill resolution works correctly for all defined profiles.
- Skill classes are correctly implemented and registered.
- No breakage in the existing compliance model structure.
</verification>

<success_criteria>
- `resolve_skills_for_profile` returns the expected skill instances for `profile_a`, `profile_b`, and `profile_c`.
- `TEXT-001` and `OCR-010` skills are correctly instantiated.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-skill-architecture/01-02-SUMMARY.md`
</output>
