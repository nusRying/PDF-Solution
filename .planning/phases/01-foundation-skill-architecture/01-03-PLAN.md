---
phase: 01-foundation-skill-architecture
plan: 03
type: execute
wave: 3
depends_on: ["01-02-PLAN.md"]
files_modified: [src/pdf_accessibility/services/remediation.py, src/pdf_accessibility/services/validation.py, src/pdf_accessibility/cli/main.py, pyproject.toml, src/pdf_accessibility/services/ingestion.py]
autonomous: true
requirements: [OPS-01, INGEST-01, INGEST-02]

must_haves:
  truths:
    - "Remediation and validation are driven by the Skill Registry instead of hardcoded loops."
    - "Jobs can be submitted and tracked via a command-line interface."
    - "Skill execution is recorded in logs and performance metrics."
  artifacts:
    - path: "src/pdf_accessibility/cli/main.py"
      provides: "Command-line interface for platform interactions"
    - path: "src/pdf_accessibility/services/remediation.py"
      provides: "Skill-aware remediation service"
    - path: "src/pdf_accessibility/services/validation.py"
      provides: "Skill-aware validation service"
  key_links:
    - from: "src/pdf_accessibility/services/ingestion.py"
      to: "src/pdf_accessibility/skills/registry.py"
      via: "Skill resolution during job execution"
    - from: "src/pdf_accessibility/cli/main.py"
      to: "src/pdf_accessibility/services/ingestion.py"
      via: "Job creation and processing triggers"
---

<objective>
Integrate the Skill Registry into the ingestion pipeline, refactoring the remediation and validation services to be rule-driven, and implement a basic CLI for job submission.

Purpose: Complete the functional path for skill-driven orchestration and provide accessible entry points for document processing.
Output: Refactored pipeline services, a functional CLI skeleton, and integrated observability.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation-skill-architecture/01-02-SUMMARY.md
@src/pdf_accessibility/services/ingestion.py
@src/pdf_accessibility/services/remediation.py
@src/pdf_accessibility/services/validation.py
@src/pdf_accessibility/core/metrics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Skill Registry into Remediation & Validation Services</name>
  <files>src/pdf_accessibility/services/remediation.py, src/pdf_accessibility/services/validation.py</files>
  <action>
    - Update `run_deterministic_remediation` to:
        - Accept `ProfileDefinition` as an argument.
        - Resolve active remediation skills via the `SkillRegistry`.
        - Iterate over resolved skills, calling `skill.remediate()`.
        - Wrap skill execution with `record_skill_execution` from `core/metrics.py`.
        - Accumulate `RemediationAction` objects from all skills.
    - Update `run_initial_validation` similarly for validation skills.
    - Remove old procedural loops and helper functions that are now encapsulated in Skills.
  </action>
  <verify>
    <automated>python -c "from pdf_accessibility.services.remediation import run_deterministic_remediation; print('Refactor complete')"</automated>
  </verify>
  <done>Remediation and validation services are refactored to be fully skill-driven.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CLI Skeleton for Job Submission</name>
  <files>src/pdf_accessibility/cli/main.py, pyproject.toml</files>
  <action>
    - Create `src/pdf_accessibility/cli/main.py` using `argparse`.
    - Implement `submit` command:
        - Arguments: `pdf_path`, `--profile`.
        - Action: Call `create_ingest_job` and `process_ingest_job`.
        - Output: Print job summary JSON to stdout.
    - Add `pdf-accessibility-cli = "pdf_accessibility.cli.main:main"` to `pyproject.toml` scripts section.
  </action>
  <verify>
    <automated>pip install -e . && pdf-accessibility-cli --help</automated>
  </verify>
  <done>Basic CLI skeleton for batch processing and job submission is available.</done>
</task>

<task type="auto">
  <name>Task 3: Update Pipeline Orchestration and Logging</name>
  <files>src/pdf_accessibility/services/ingestion.py</files>
  <action>
    - Update `process_ingest_job` to pass the `ProfileDefinition` (using `get_profile_definition(job.compliance_profile)`) to remediation and validation services.
    - Ensure structured logging (via `structlog`) records:
        - Start/finish of skill execution blocks.
        - List of active skills for the current profile.
  </action>
  <verify>
    <automated>python -c "from pdf_accessibility.services.ingestion import process_ingest_job; print('Orchestrator updated')"</automated>
  </verify>
  <done>Pipeline orchestration correctly routes documents through skill-driven processing with full observability.</done>
</task>

</tasks>

<verification>
- Full pipeline run (via CLI or tests) successfully remediates and validates a document using Skills.
- Prometheus metrics are updated during a job run.
- CLI correctly interacts with the ingestion orchestrator.
</verification>

<success_criteria>
- `pdf-accessibility-cli submit path/to/sample.pdf` completes successfully.
- Job summary shows non-zero remediation actions (from TEXT-001).
- Logs show skill execution events.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-skill-architecture/01-03-SUMMARY.md`
</output>
