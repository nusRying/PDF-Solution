---
phase: 06-operations-hardening
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: [
  "scripts/benchmark.py"
]
autonomous: true
requirements: [OPS-04, OPS-07]

must_haves:
  truths:
    - "Benchmark script successfully submits a batch of PDFs"
    - "Script correctly calculates total pages processed and time taken"
    - "Performance report includes pages/second metric"
  artifacts:
    - path: "scripts/benchmark.py"
      provides: "Performance measurement tool"
---

<objective>
Develop a performance benchmarking tool to measure system throughput and verify performance targets.

Purpose: Quantify system performance and ensure throughput targets are met.
Output: Benchmark script and performance report.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-operations-hardening/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Benchmarking Script</name>
  <files>scripts/benchmark.py</files>
  <action>
    - Develop `scripts/benchmark.py` that accepts a directory of PDF files as input.
    - Script logic:
      - Scan directory for PDF files.
      - Use `httpx` to upload files in parallel (up to a concurrency limit).
      - Poll for job completion using the job IDs.
      - Track start and end time for each job.
      - Extract page counts from JobRecords or artifacts to calculate total pages.
      - Report: Total PDFs, Total Pages, Total Time, Average Pages/Second.
    - Ensure it uses the API Key if security is enabled.
  </action>
  <verify>
    <automated>python scripts/benchmark.py --help</automated>
  </verify>
  <done>Benchmark script is functional and ready for execution.</done>
</task>

<task type="auto">
  <name>Task 2: Conduct Benchmark and Document Results</name>
  <files>scripts/benchmark.py</files>
  <action>
    - Run the benchmark script against a standard set of PDFs (e.g., from `data/originals`).
    - Observe the system behavior using `docker stats` to ensure resources are utilized efficiently.
    - Document the results in a new file `BENCHMARK_RESULTS.md` in the project root.
    - Compare results against the target: >= 30 pages/sec.
  </action>
  <verify>
    <automated>ls BENCHMARK_RESULTS.md</automated>
  </verify>
  <done>Performance has been measured and results are documented.</done>
</task>

</tasks>

<verification>
Run `python scripts/benchmark.py data/originals --api-key your-key`.
Check `BENCHMARK_RESULTS.md` for final numbers.
</verification>

<success_criteria>
- Throughput >= 30 pages/sec (target).
- Benchmark script is robust and reusable.
</success_criteria>

<output>
After completion, create `.planning/phases/06-operations-hardening/06-03-SUMMARY.md`
</output>
