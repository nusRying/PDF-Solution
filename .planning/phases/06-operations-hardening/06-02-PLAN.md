---
phase: 06-operations-hardening
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [
  "src/pdf_accessibility/services/job_queue.py",
  "src/pdf_accessibility/main.py",
  "src/pdf_accessibility/api/security.py",
  "src/pdf_accessibility/core/celery.py",
  "docker-compose.yml",
  "pyproject.toml"
]
autonomous: true
requirements: [OPS-05, OPS-08]

must_haves:
  truths:
    - "Documents are processed by a dedicated Celery worker process"
    - "API requests without a valid API key are rejected"
    - "CORS headers are correctly returned for configured origins"
  artifacts:
    - path: "src/pdf_accessibility/core/celery.py"
      provides: "Celery application configuration"
    - path: "src/pdf_accessibility/api/security.py"
      provides: "FastAPI security dependencies"
  key_links:
    - from: "src/pdf_accessibility/services/job_queue.py"
      to: "Celery Task"
      via: "task.delay()"
---

<objective>
Replace the in-memory queue with a production-ready Celery/Redis setup and apply security hardening.

Purpose: Enable horizontal scaling of workers and secure the API.
Output: Celery integration and security middleware.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-operations-hardening/06-CONTEXT.md
@src/pdf_accessibility/services/job_queue.py
@src/pdf_accessibility/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Celery/Redis Integration</name>
  <files>src/pdf_accessibility/core/celery.py, src/pdf_accessibility/services/job_queue.py, pyproject.toml</files>
  <action>
    - Add `celery` and `redis` to `pyproject.toml`.
    - Create `src/pdf_accessibility/core/celery.py` to initialize the Celery app with Redis broker and backend.
    - Define a task `process_document_task` that wraps `src.pdf_accessibility.services.ingestion.process_ingest_job`.
    - Refactor `src/pdf_accessibility/services/job_queue.py`:
      - Remove threading/internal queue logic.
      - Update `enqueue(job_id)` to call `process_document_task.delay(job_id)`.
  </action>
  <verify>
    <automated>celery -A pdf_accessibility.core.celery inspect ping</automated>
  </verify>
  <done>JobQueueService now delegates to Celery for asynchronous processing.</done>
</task>

<task type="auto">
  <name>Task 2: Configure API Security and CORS</name>
  <files>src/pdf_accessibility/api/security.py, src/pdf_accessibility/main.py, src/pdf_accessibility/core/settings.py</files>
  <action>
    - Update `Settings` with `API_KEYS` (list) and `ALLOWED_ORIGINS` (list).
    - Create `src/pdf_accessibility/api/security.py`:
      - Implement a FastAPI `APIKeyHeader` dependency to validate the API key.
    - Update `src/pdf_accessibility/main.py`:
      - Add `CORSMiddleware`.
      - Apply the security dependency globally or to specific routers (excluding health checks).
  </action>
  <verify>
    <automated>pytest tests/test_health.py -v</automated>
  </verify>
  <done>API is secured with API keys and CORS is configured.</done>
</task>

<task type="auto">
  <name>Task 3: Update Docker Compose for Workers</name>
  <files>docker-compose.yml</files>
  <action>
    - Add `redis` service to `docker-compose.yml`.
    - Add `worker` service to `docker-compose.yml` (using same image as `api` but different entrypoint: `celery -A pdf_accessibility.core.celery worker`).
    - Ensure `api` service has environment variables to connect to `redis`.
  </action>
  <verify>
    <automated>docker compose config</automated>
  </verify>
  <done>Full stack (API, Worker, Redis, MinIO) is ready for orchestration.</done>
</task>

</tasks>

<verification>
Start full stack with `docker compose up`. Test job submission and verify worker processing. Test unauthorized API access.
</verification>

<success_criteria>
- Jobs processed by Celery worker.
- API Key authentication working.
- Full stack runs in Docker.
</success_criteria>

<output>
After completion, create `.planning/phases/06-operations-hardening/06-02-SUMMARY.md`
</output>
