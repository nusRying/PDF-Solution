---
phase: 06-operations-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/pdf_accessibility/services/file_store.py",
  "src/pdf_accessibility/services/s3_file_store.py",
  "src/pdf_accessibility/core/settings.py",
  "Dockerfile",
  "docker-compose.yml",
  "pyproject.toml"
]
autonomous: true
requirements: [OPS-02, OPS-06]

must_haves:
  truths:
    - "Application runs successfully inside a Docker container"
    - "docker-compose up starts the API and a MinIO (S3-compatible) container"
    - "Documents can be stored and retrieved using S3-compatible storage"
  artifacts:
    - path: "Dockerfile"
      provides: "Containerized application environment"
    - path: "docker-compose.yml"
      provides: "Orchestration for API and dependencies"
    - path: "src/pdf_accessibility/services/s3_file_store.py"
      provides: "S3-compatible storage implementation"
  key_links:
    - from: "src/pdf_accessibility/core/settings.py"
      to: "src/pdf_accessibility/services/file_store.py"
      via: "factory method or dependency injection"
---

<objective>
Refactor storage to support S3-compatible backends and dockerize the application with all system dependencies.

Purpose: Enable cloud-native deployment and consistent execution environments.
Output: Dockerized application and S3 storage connector.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-operations-hardening/06-CONTEXT.md
@src/pdf_accessibility/services/file_store.py
@src/pdf_accessibility/core/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor FileStore to support multiple backends</name>
  <files>src/pdf_accessibility/services/file_store.py, src/pdf_accessibility/core/settings.py</files>
  <action>
    - Refactor `src/pdf_accessibility/services/file_store.py`:
      - Introduce `BaseFileStore` abstract base class defining all storage methods.
      - Rename current `FileStore` to `LocalFileStore` (inheriting from `BaseFileStore`).
    - Update `src/pdf_accessibility/core/settings.py`:
      - Add `STORAGE_BACKEND` (default: "local") and S3 configuration (bucket, endpoint, access/secret keys).
      - Implement a factory method or update dependency injection to return the appropriate store instance based on `STORAGE_BACKEND`.
    - Update all service references to use the base class or the new factory.
  </action>
  <verify>
    <automated>pytest tests/test_models_layout.py -v</automated>
  </verify>
  <done>Local storage still works exactly as before after refactor.</done>
</task>

<task type="auto">
  <name>Task 2: Implement S3FileStore and update dependencies</name>
  <files>src/pdf_accessibility/services/s3_file_store.py, pyproject.toml</files>
  <action>
    - Add `boto3` to `pyproject.toml`.
    - Implement `S3FileStore` in `src/pdf_accessibility/services/s3_file_store.py` inheriting from `BaseFileStore`.
    - Ensure it handles bucket creation (if not exists) and uses the S3 configuration from `Settings`.
    - Support `S3_ENDPOINT_URL` for MinIO compatibility.
  </action>
  <verify>
    <automated>pytest tests/test_health.py -v</automated>
  </verify>
  <done>S3FileStore is implemented and passes basic linting; ready for integration test.</done>
</task>

<task type="auto">
  <name>Task 3: Dockerize the application</name>
  <files>Dockerfile, docker-compose.yml</files>
  <action>
    - Create a multi-stage `Dockerfile`:
      - Install system dependencies: `tesseract-ocr`, `libpoppler-cpp-dev`, `python3-tk`, `libgl1`.
      - Install Python dependencies using `pip`.
      - Use a slim base image for the final stage.
    - Create `docker-compose.yml`:
      - Service `api`: builds from `Dockerfile`, environment vars for storage.
      - Service `minio`: S3-compatible storage for local testing.
      - Service `createbuckets`: helper to create the initial S3 bucket in MinIO.
  </action>
  <verify>
    <automated>docker compose config</automated>
  </verify>
  <done>Docker configuration is valid and ready for 'docker compose up'.</done>
</task>

</tasks>

<verification>
Check that the app starts in Docker and can store files in MinIO if configured.
</verification>

<success_criteria>
- App runs in Docker.
- LocalFileStore works.
- S3FileStore is implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/06-operations-hardening/06-01-SUMMARY.md`
</output>
