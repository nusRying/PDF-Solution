---
phase: 04-validation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pdf_accessibility/models/validation.py
  - src/pdf_accessibility/models/canonical.py
  - src/pdf_accessibility/models/documents.py
  - src/pdf_accessibility/services/pdf_parser.py
  - src/pdf_accessibility/services/canonicalize.py
  - src/pdf_accessibility/services/validation_catalog.py
autonomous: true
requirements:
  - VALID-01
---

<objective>
Enhance the validation foundation by adding structural anchors to findings, expanding document models to capture low-level accessibility metadata, and implementing a Rule Catalog service for centralized rule management.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/pdf_accessibility/models/validation.py
@src/pdf_accessibility/models/canonical.py
@src/pdf_accessibility/services/pdf_parser.py
@src/pdf_accessibility/services/canonicalize.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enhance models for precise reporting</name>
  <files>src/pdf_accessibility/models/validation.py, src/pdf_accessibility/models/canonical.py, src/pdf_accessibility/models/documents.py</files>
  <behavior>
    - ValidationFinding should have optional block_id (str) and bbox (BoundingBox) fields.
    - CanonicalBlock should have an optional alt_text (str) field.
    - CanonicalMetadata should have boolean fields: is_tagged, has_struct_tree, is_pdf_ua_identifier_present.
    - ParserDocumentMetadata should have corresponding fields for raw extraction.
  </behavior>
  <action>
    - Update ValidationFinding in src/pdf_accessibility/models/validation.py to include block_id and bbox.
    - Update CanonicalBlock in src/pdf_accessibility/models/canonical.py to include alt_text.
    - Add accessibility flags to CanonicalMetadata and ParserDocumentMetadata.
  </action>
  <verify>
    <automated>pytest tests/test_models_validation_enhanced.py</automated>
  </verify>
  <done>Models updated and serializable with new fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract low-level accessibility metadata in parser</name>
  <files>src/pdf_accessibility/services/pdf_parser.py, src/pdf_accessibility/services/canonicalize.py</files>
  <behavior>
    - parse_pdf should detect if MarkInfo/Marked is True.
    - parse_pdf should detect if StructTreeRoot exists.
    - parse_pdf should check XMP metadata for PDF/UA-1 identification (using pikepdf as it's better for low-level dictionary access).
    - canonicalize_document should map these flags to the CanonicalDocument.
  </behavior>
  <action>
    - Integrate pikepdf in src/pdf_accessibility/services/pdf_parser.py to inspect the document catalog.
    - Extract MarkInfo, StructTreeRoot presence, and XMP PDF/UA version.
    - Update canonicalize.py to populate the new CanonicalMetadata flags.
  </action>
  <verify>
    <automated>pytest tests/test_pdf_parser_accessibility_meta.py</automated>
  </verify>
  <done>Parser extracts low-level accessibility flags from PDFs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Rule Catalog Service</name>
  <files>src/pdf_accessibility/services/validation_catalog.py</files>
  <behavior>
    - RuleCatalog should provide a way to look up rule details by rule_id.
    - Details include: name, description, default severity, and list of StandardMapping (Matterhorn/WCAG).
    - Initial catalog should include entries for planned Matterhorn/WCAG rules.
  </behavior>
  <action>
    - Create RuleCatalog service in src/pdf_accessibility/services/validation_catalog.py.
    - Define a static or configuration-driven map of rule definitions.
    - Implement a lookup method that returns a RuleDefinition model.
  </action>
  <verify>
    <automated>pytest tests/test_rule_catalog.py</automated>
  </verify>
  <done>RuleCatalog service is functional and populated with initial rule metadata.</done>
</task>

</tasks>

<verification>
Verify that ValidationFindings can now report exactly which block failed and where it is on the page, and that the system can detect basic PDF/UA compliance flags from the source PDF.
</verification>

<success_criteria>
- ValidationFinding supports block_id and bbox.
- PdfParser detects MarkInfo and StructTreeRoot.
- RuleCatalog returns metadata for at least 5 rule IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-engine/04-01-SUMMARY.md`
</output>
