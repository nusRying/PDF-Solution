---
phase: 04-validation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pdf_accessibility/models/validation.py
  - src/pdf_accessibility/models/canonical.py
  - src/pdf_accessibility/models/documents.py
  - src/pdf_accessibility/services/pdf_parser.py
  - src/pdf_accessibility/services/rule_catalog.py
autonomous: true
requirements:
  - VALID-01
  - VALID-04
must_haves:
  truths:
    - "Validation findings include precise block IDs and bounding boxes for spatial anchoring."
    - "The PDF parser identifies low-level accessibility indicators: MarkInfo, StructTreeRoot, and XMP PDF/UA-1 part identifiers."
    - "The Rule Catalog maps internal validation rules to specific Matterhorn Protocol and WCAG 2.1 criteria."
    - "Document-level metadata (tagging status, structure tree presence) is propagated to the validation engine."
  artifacts:
    - path: "src/pdf_accessibility/models/validation.py"
      provides: "ValidationFinding with spatial fields"
    - path: "src/pdf_accessibility/services/pdf_parser.py"
      provides: "Accessibility flag extraction logic"
    - path: "src/pdf_accessibility/services/rule_catalog.py"
      provides: "Standard-mapped rule definitions"
  key_links:
    - from: "src/pdf_accessibility/services/pdf_parser.py"
      to: "src/pdf_accessibility/models/documents.py"
      via: "ParserDocumentMetadata population"
    - from: "src/pdf_accessibility/services/rule_catalog.py"
      to: "src/pdf_accessibility/models/validation.py"
      via: "ValidationSeverity and StandardMapping"
---

<objective>
Harden the validation infrastructure by providing precise spatial anchors for findings, extracting low-level PDF accessibility metadata, and establishing a standard-mapped Rule Catalog for Matterhorn and WCAG compliance.

Purpose: Enables pinpointing accessibility failures on the page and provides the necessary context for document-level compliance checks.
Output: Enhanced models, updated parser with low-level inspection, and a functional Rule Catalog service.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pdf_accessibility/models/validation.py
@src/pdf_accessibility/models/canonical.py
@src/pdf_accessibility/models/documents.py
@src/pdf_accessibility/services/pdf_parser.py
@src/pdf_accessibility/services/rule_catalog.py

<interfaces>
From src/pdf_accessibility/models/validation.py:
```python
class ValidationSeverity(str, Enum):
    info, warning, error

class StandardMapping(BaseModel):
    standard: ComplianceStandard
    rule_id: str
    criterion: str | None

class ValidationFinding(BaseModel):
    rule_id: str
    severity: ValidationSeverity
    message: str
    page_number: int | None
    block_id: str | None
    bbox: BoundingBox | None
    source: str
    standards: list[StandardMapping]
```

From src/pdf_accessibility/models/documents.py:
```python
class ParserDocumentMetadata(BaseModel):
    # ... standard fields ...
    is_tagged: bool = False
    has_struct_tree: bool = False
    is_pdf_ua_identifier_present: bool = False
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enhance models for precise spatial reporting</name>
  <files>src/pdf_accessibility/models/validation.py, src/pdf_accessibility/models/canonical.py, src/pdf_accessibility/models/documents.py</files>
  <behavior>
    - ValidationFinding should include optional `block_id` (str) and `bbox` (BoundingBox) fields.
    - CanonicalMetadata and ParserDocumentMetadata should include boolean flags: `is_tagged`, `has_struct_tree`, `is_pdf_ua_identifier_present`.
    - CanonicalBlock should include an optional `alt_text` (str) field for figure/image validation.
  </behavior>
  <action>
    - Update ValidationFinding in src/pdf_accessibility/models/validation.py to add `block_id` and `bbox` fields.
    - Update CanonicalMetadata (src/pdf_accessibility/models/canonical.py) and ParserDocumentMetadata (src/pdf_accessibility/models/documents.py) with the three accessibility flags.
    - Update CanonicalBlock (src/pdf_accessibility/models/canonical.py) to include `alt_text`.
    - Reference: Focus 2 (structural anchors) and Focus 5 (PDF metadata access).
  </action>
  <verify>
    <automated>pytest tests/test_models_validation_enhanced.py</automated>
  </verify>
  <done>Models are updated to support spatial anchoring and low-level accessibility flags.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract low-level PDF accessibility flags in Parser</name>
  <files>src/pdf_accessibility/services/pdf_parser.py</files>
  <behavior>
    - `parse_pdf` must use pikepdf to inspect the Document Catalog.
    - It should detect the presence of `/MarkInfo` with `/Marked true` (is_tagged).
    - It should detect the presence of `/StructTreeRoot` (has_struct_tree).
    - It should inspect XMP metadata for `pdfuaid:part` or the PDF/UA identification namespace URI (is_pdf_ua_identifier_present).
  </behavior>
  <action>
    - Implement `_extract_low_level_metadata(pdf_path: Path)` using pikepdf.
    - Integrate this helper into the main `parse_pdf` flow in src/pdf_accessibility/services/pdf_parser.py.
    - Populates the new flags in `ParserDocumentMetadata`.
    - Reference: Focus 3 (low-level PDF flags).
  </action>
  <verify>
    <automated>pytest tests/test_pdf_parser_accessibility_meta.py</automated>
  </verify>
  <done>PdfParser successfully extracts low-level accessibility indicators from source documents.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate RuleCatalog with Matterhorn/WCAG mappings</name>
  <files>src/pdf_accessibility/services/rule_catalog.py</files>
  <behavior>
    - `RuleInfo` model should include optional `matterhorn_mapping` (str) and `wcag_mapping` (str) fields.
    - `RuleCatalog` should store and provide these mappings for all registered validation rules.
    - Initial rule set should include mappings for common structural and metadata checks (e.g. Headings, Figures, Title).
  </behavior>
  <action>
    - Update `RuleInfo` in src/pdf_accessibility/services/rule_catalog.py to include mapping fields.
    - Populate the `_rules` dictionary in `RuleCatalog` with initial rules and their corresponding Matterhorn/WCAG references.
    - Reference: Focus 4 (standard mapping logic).
  </action>
  <verify>
    <automated>pytest tests/test_rule_catalog.py</automated>
  </verify>
  <done>RuleCatalog provides standardized mappings for validation rules.</done>
</task>

</tasks>

<verification>
Verify that spatial anchors and low-level flags are correctly propagated from the parser to validation findings.
1. Run parser on a known tagged PDF and check for metadata flags.
2. Generate a validation finding and ensure it serializes with bbox/block_id.
3. Query the rule catalog for a rule and verify its Matterhorn/WCAG mapping.
</verification>

<success_criteria>
- ValidationFinding includes block_id and bbox.
- Parser detects is_tagged and has_struct_tree.
- RuleCatalog provides Matterhorn mappings for at least 5 rules.
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-engine/04-01-SUMMARY.md`
</output>
