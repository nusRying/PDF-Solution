---
phase: 04-validation-engine
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/pdf_accessibility/services/reporting.py
  - src/pdf_accessibility/services/external_validators.py
  - src/pdf_accessibility/cli/main.py
autonomous: true
requirements:
  - VALID-04
---

<objective>
Enable machine-readable reporting by implementing an EARL (Evaluation and Report Language) generator and provide a service for integrating results from external validators like PAC.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-validation-engine/04-02-SUMMARY.md
@src/pdf_accessibility/models/validation.py
@src/pdf_accessibility/services/validation.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement EARL Report Generator</name>
  <files>src/pdf_accessibility/services/reporting.py</files>
  <behavior>
    - EARLReportGenerator should take a ValidationArtifact and produce a machine-readable JSON-LD EARL report.
    - Assertions should map rule_id to EARL test criteria.
    - Test results (pass/fail/warning) should be correctly encoded in EARL vocabulary.
    - Subjects should represent the document being tested.
  </behavior>
  <action>
    - Create reporting.py and implement the EARLReportGenerator class.
    - Define EARL vocabulary mappings for internal validation severities and statuses.
    - Implement a generate_earl method that returns a JSON-LD dict.
  </action>
  <verify>
    <automated>pytest tests/test_earl_reporting.py</automated>
  </verify>
  <done>EARL report generation is functional and adheres to standard EARL vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PAC Integration Service</name>
  <files>src/pdf_accessibility/services/external_validators.py</files>
  <behavior>
    - PACIntegrationService should allow recording validation outcomes from the PAC tool.
    - It should support a mock mode (returning success/failure) and a skeleton for CLI parsing.
    - Outcomes should be convertible into internal ValidationFindings or wrapped in a specific ExternalValidationArtifact.
  </behavior>
  <action>
    - Create external_validators.py and implement PACIntegrationService.
    - Provide methods to run external validation and parse results (start with mock/stub).
    - Map PAC error codes to internal Matterhorn rule IDs where possible.
  </action>
  <verify>
    <automated>pytest tests/test_pac_integration.py</automated>
  </verify>
  <done>PAC Integration service implemented with mock support.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLI for EARL output</name>
  <files>src/pdf_accessibility/cli/main.py</files>
  <action>
    - Add a --report-format option to the validate and process commands.
    - Options: json (default), earl.
    - Use EARLReportGenerator when earl format is requested and save to the specified output directory.
  </action>
  <verify>
    <automated>python -m pdf_accessibility.cli.main validate --report-format earl --output reports/ test.pdf</automated>
  </verify>
  <done>CLI supports generating EARL reports.</done>
</task>

</tasks>

<verification>
Run a full validation via CLI with --report-format earl and verify that the output file contains valid EARL JSON-LD with all detected findings.
</verification>

<success_criteria>
- EARL reports can be generated for any validation job.
- PAC integration service can record external findings.
- CLI provides user-friendly access to EARL reporting.
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-engine/04-03-SUMMARY.md`
</output>
