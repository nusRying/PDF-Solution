---
phase: 07-structural-tagging-pdf-ua-write-back
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/pdf_accessibility/services/tagging.py]
autonomous: true
requirements: [REMED-01, CORE-04]
must_haves:
  truths:
    - "TaggingEngine can map CanonicalRole to standard PDF tags"
    - "TaggingEngine can build a StructTreeRoot in a pikepdf document"
    - "TaggingEngine correctly nests LI under L"
  artifacts:
    - path: "src/pdf_accessibility/services/tagging.py"
      provides: "TaggingEngine class with structural building logic"
  key_links:
    - from: "CanonicalRole"
      to: "PDF Standard Tags"
      via: "role_map dictionary"
---

<objective>
Implement the core `TaggingEngine` that builds the PDF Structure Tree from the `CanonicalDocumentModel`.
Purpose: To provide a robust way to generate a standards-compliant tag tree.
Output: `src/pdf_accessibility/services/tagging.py` with initial structural mapping.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pdf_accessibility/models/canonical.py

<interfaces>
From src/pdf_accessibility/models/canonical.py:
```python
class CanonicalRole(str, Enum):
    heading1 = "heading1"
    heading2 = "heading2"
    heading3 = "heading3"
    heading4 = "heading4"
    heading5 = "heading5"
    heading6 = "heading6"
    list = "list"
    list_item = "list_item"
    table = "table"
    figure = "figure"
    caption = "caption"
    text = "text"
    artifact = "artifact"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement TaggingEngine and Role Mapping</name>
  <files>src/pdf_accessibility/services/tagging.py, tests/test_tagging_engine.py</files>
  <behavior>
    - TaggingEngine.map_role_to_tag("heading1") returns "/H1"
    - TaggingEngine.map_role_to_tag("text") returns "/P"
    - TaggingEngine.map_role_to_tag("list") returns "/L"
    - TaggingEngine.map_role_to_tag("list_item") returns "/LI"
  </behavior>
  <action>
    Create `src/pdf_accessibility/services/tagging.py` with `TaggingEngine` class.
    Implement `map_role_to_tag` method that translates `CanonicalRole` to standard PDF/UA tags.
    Use a mapping dictionary for efficiency (D-07-02).
  </action>
  <verify>
    <automated>pytest tests/test_tagging_engine.py</automated>
  </verify>
  <done>Role mapping is fully implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Structural Tree Building with Logical Nesting</name>
  <files>src/pdf_accessibility/services/tagging.py</files>
  <action>
    Implement `build_struct_tree(pdf: pikepdf.Pdf, doc: CanonicalDocument)` in `TaggingEngine`.
    - Initialize `/StructTreeRoot` in `pdf.Root`.
    - Iterate through pages and blocks in `doc`.
    - Create `StructElem` nodes for each block.
    - Implement logical nesting for lists: ensure all `LI` elements are children of an `L` element.
    - Handle H1-H6 hierarchy (flat sequence for now, but following the tag order).
  </action>
  <verify>
    <automated>pytest tests/test_tagging_engine.py -k "test_build_struct_tree"</automated>
  </verify>
  <done>Structural tree building logic supports logical nesting for lists.</done>
</task>

</tasks>

<verification>
Ensure TaggingEngine correctly builds a StructTreeRoot in a blank pikepdf document and preserves logical relationships.
</verification>

<success_criteria>
- `TaggingEngine` correctly maps roles to tags.
- List items are correctly nested under List elements.
- Structure tree is successfully added to the pikepdf Document.
</success_criteria>

<output>
After completion, create `.planning/phases/07-structural-tagging-pdf-ua-write-back/07-01-SUMMARY.md`
</output>
