---
phase: 07-structural-tagging-pdf-ua-write-back
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [src/pdf_accessibility/services/tagging.py]
autonomous: true
requirements: [CORE-04, REMED-01]
must_haves:
  truths:
    - "Content stream is correctly marked with BDC/EMC operators"
    - "Each content element is assigned a unique MCID per page"
    - "MCID is correctly mapped in the Structure Tree ParentTree"
  artifacts:
    - path: "src/pdf_accessibility/services/tagging.py"
      provides: "mark_content method for content stream edits"
  key_links:
    - from: "Structure Element"
      to: "Content Stream"
      via: "MCID"
---

<objective>
Enable content marking and MCID assignment to connect structure tree elements with actual content on pages.
Purpose: To fulfill the PDF/UA requirement for marked content association.
Output: Updated `TaggingEngine` with content marking capabilities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pdf_accessibility/services/tagging.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Content Stream Marking (BDC/EMC)</name>
  <files>src/pdf_accessibility/services/tagging.py</files>
  <action>
    Implement `mark_content_on_page(pdf_page: pikepdf.Page, canonical_page: CanonicalPage)` in `TaggingEngine`.
    - Iterate through `canonical_page.blocks`.
    - Use `pikepdf` to wrap the corresponding content in the page's `/Contents` stream.
    - Insert `BDC` (Begin Marked Content) and `EMC` (End Marked Content) operators.
    - Set the tag in BDC to match the block's standard tag (from map_role_to_tag).
    - Assign a unique `MCID` in the BDC property list.
  </action>
  <verify>
    <automated>pytest tests/test_tagging_engine.py -k "test_mark_content"</automated>
  </verify>
  <done>Content streams are successfully marked with BDC/EMC and MCIDs.</done>
</task>

<task type="auto">
  <name>Task 2: Associate MCIDs in the Structure Tree ParentTree</name>
  <files>src/pdf_accessibility/services/tagging.py</files>
  <action>
    Update `build_struct_tree` to integrate `mark_content_on_page`.
    - Populate the `/ParentTree` in `StructTreeRoot` to map `MCID`s to their corresponding `StructElem` objects.
    - Ensure the `/K` (Kids) array in `StructElem` contains the `MCID` integer for the corresponding content.
  </action>
  <verify>
    <automated>pytest tests/test_tagging_engine.py -k "test_parent_tree"</automated>
  </verify>
  <done>ParentTree correctly maps MCIDs to Structure Elements.</done>
</task>

</tasks>

<verification>
Ensure content marking and MCID mapping result in a valid Structure Tree that PDF viewers can traverse.
</verification>

<success_criteria>
- BDC/EMC operators appear in the PDF content stream.
- MCIDs are correctly incremented per page.
- The ParentTree links MCIDs back to structure elements.
</success_criteria>

<output>
After completion, create `.planning/phases/07-structural-tagging-pdf-ua-write-back/07-02-SUMMARY.md`
</output>
