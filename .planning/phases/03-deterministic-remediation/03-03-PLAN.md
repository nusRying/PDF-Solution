---
phase: 03-deterministic-remediation
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [
  "src/pdf_accessibility/services/pdf_writer.py",
  "src/pdf_accessibility/models/compliance.py",
  "src/pdf_accessibility/services/remediation.py",
  "tests/test_pipeline_integration.py"
]
autonomous: true
requirements: [CORE-04, SKILL-03, REMED-01]

must_haves:
  truths:
    - "A new PDF is generated with updated title, author, and language metadata."
    - "New remediation skills are active in the remediation pipeline based on Compliance Profiles."
  artifacts:
    - path: "src/pdf_accessibility/services/pdf_writer.py"
      provides: "PdfWriterService"
  key_links:
    - from: "src/pdf_accessibility/services/remediation.py"
      to: "src/pdf_accessibility/skills/remediation/headings.py"
      via: "registration"
    - from: "src/pdf_accessibility/services/pdf_writer.py"
      to: "pikepdf"
      via: "library calls"
---

<objective>
Implement the PDF Writer service and integrate the full remediation pipeline.
Purpose: Complete the loop by producing a remediated PDF file and ensuring all deterministic skills are active in production profiles.
Output: A functional PDF writer and updated pipeline orchestrator.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-deterministic-remediation/03-CONTEXT.md
@src/pdf_accessibility/services/remediation.py
@src/pdf_accessibility/models/compliance.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PdfWriterService using pikepdf</name>
  <files>src/pdf_accessibility/services/pdf_writer.py, tests/test_pipeline_integration.py</files>
  <behavior>
    - Load an original PDF using `pikepdf`.
    - Apply metadata changes (title, author, language) to XMP and Info dictionaries.
    - Set the `ViewerPreferences /DisplayDocTitle true` flag.
    - Save the modified PDF to an output path.
  </behavior>
  <action>
    Create `PdfWriterService` in `src/pdf_accessibility/services/pdf_writer.py`.
    Implement `write_remediated_pdf(original_pdf_path, canonical_doc, output_path)` method.
    Use `pikepdf` to manipulate the document object.
  </action>
  <verify>
    <automated>pytest tests/test_pipeline_integration.py -k test_pdf_writer</automated>
  </verify>
  <done>PdfWriterService generates a valid PDF with correct metadata from CanonicalDocument.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate New Skills into Compliance Profiles</name>
  <files>src/pdf_accessibility/models/compliance.py, src/pdf_accessibility/services/remediation.py</files>
  <action>
    - Update `PROFILE_DEFINITIONS` in `src/pdf_accessibility/models/compliance.py` to include new skill IDs (`REMED-HEAD-001`, `REMED-LIST-001`, `REMED-META-001`, `REMED-ART-001`) in `remediation_skill_ids` for all profiles.
    - Import and register the 4 new skills in `src/pdf_accessibility/services/remediation.py` within the global `_registry` initialization block.
  </action>
  <verify>
    <automated>python -c "from pdf_accessibility.services.remediation import _registry; print([s.skill_id for s in _registry.get_all_remediation()])"</automated>
  </verify>
  <done>New skills are registered and activated via ComplianceProfiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Full Pipeline Integration Test</name>
  <files>tests/test_pipeline_integration.py</files>
  <behavior>
    - Load a sample PDF.
    - Run the remediation pipeline using a ComplianceProfile.
    - Verify that multiple remediation actions are generated (Headings, Lists, etc.).
    - Pass the result to `PdfWriterService` and check the resulting file.
  </behavior>
  <action>
    Create a comprehensive integration test that runs the full flow from CanonicalDocument generation (mocked or real) to remediated PDF output.
  </action>
  <verify>
    <automated>pytest tests/test_pipeline_integration.py -k test_full_remediation_flow</automated>
  </verify>
  <done>Full remediation pipeline produces a remediated PDF with audit trail.</done>
</task>

</tasks>

<success_criteria>
- [ ] PdfWriterService correctly writes structural metadata.
- [ ] New skills are fully integrated into the platform's orchestration layer.
- [ ] Integration tests verify the end-to-end flow.
</success_criteria>

<output>
After completion, create `.planning/phases/03-deterministic-remediation/03-03-SUMMARY.md`
</output>
