---
phase: 03-deterministic-remediation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [
  "src/pdf_accessibility/models/canonical.py",
  "src/pdf_accessibility/skills/remediation/metadata.py",
  "src/pdf_accessibility/skills/remediation/artifacts.py",
  "tests/test_remediation_metadata.py"
]
autonomous: true
requirements: [REMED-01, SKILL-03]

must_haves:
  truths:
    - "Document metadata (title, author, language) is populated in the Canonical Document Model."
    - "Repetitive headers and footers are correctly identified and assigned the 'artifact' role."
  artifacts:
    - path: "src/pdf_accessibility/skills/remediation/metadata.py"
      provides: "MetadataRepairSkill"
    - path: "src/pdf_accessibility/skills/remediation/artifacts.py"
      provides: "ArtifactClassificationSkill"
  key_links:
    - from: "src/pdf_accessibility/skills/remediation/metadata.py"
      to: "src/pdf_accessibility/models/canonical.py"
      via: "field updates"
---

<objective>
Implement metadata repair and artifact classification skills.
Purpose: Ensure essential document-level metadata exists and repetitive non-content elements are hidden from screen readers.
Output: Two functional remediation skills and updated document models.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-deterministic-remediation/03-CONTEXT.md
@src/pdf_accessibility/models/canonical.py
@src/pdf_accessibility/skills/base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CanonicalDocument model for Metadata</name>
  <files>src/pdf_accessibility/models/canonical.py</files>
  <action>
    Add `title: str | None = None`, `author: str | None = None`, and `language: str = "en"` to the `CanonicalDocument` pydantic model.
    Default language to "en".
  </action>
  <verify>
    <automated>python -c "from pdf_accessibility.models.canonical import CanonicalDocument; print('title' in CanonicalDocument.model_fields)"</automated>
  </verify>
  <done>CanonicalDocument model updated with metadata fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MetadataRepairSkill</name>
  <files>src/pdf_accessibility/skills/remediation/metadata.py, tests/test_remediation_metadata.py</files>
  <behavior>
    - If title is missing, search for the first H1 or largest text block and set it as the document title.
    - If author is missing, look for common patterns (e.g., "By [Name]") or default to a system placeholder if requested.
    - Ensure language is set (default "en").
  </behavior>
  <action>
    Create `MetadataRepairSkill` class.
    Implement `remediate` method:
    - Update `canonical_doc.title`, `canonical_doc.author`, `canonical_doc.language`.
    - Record changes in `RemediationAction`.
  </action>
  <verify>
    <automated>pytest tests/test_remediation_metadata.py -k test_metadata_repair</automated>
  </verify>
  <done>Document metadata is correctly populated if missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement ArtifactClassificationSkill</name>
  <files>src/pdf_accessibility/skills/remediation/artifacts.py, tests/test_remediation_metadata.py</files>
  <behavior>
    - Identify blocks that repeat exactly (or nearly exactly) at the same y-coordinates on multiple pages (headers/footers).
    - Change role to 'artifact'.
  </behavior>
  <action>
    Create `ArtifactClassificationSkill` class.
    Implement `remediate` method:
    - Use a frequency map of (text, y0) pairs across pages to find repetitive elements.
    - Update `role` of matching blocks to `artifact`.
    - Record changes in `RemediationAction`.
  </action>
  <verify>
    <automated>pytest tests/test_remediation_metadata.py -k test_artifact_classification</automated>
  </verify>
  <done>Repetitive headers and footers are marked as artifacts.</done>
</task>

</tasks>

<success_criteria>
- [ ] CanonicalDocument supports title, author, and language.
- [ ] MetadataRepairSkill populates missing title/author.
- [ ] ArtifactClassificationSkill correctly identifies and tags headers/footers.
</success_criteria>

<output>
After completion, create `.planning/phases/03-deterministic-remediation/03-02-SUMMARY.md`
</output>
