---
phase: 02-parsing-layout-foundation
plan: 03
type: execute
wave: 3
depends_on: [02-parsing-layout-foundation-02]
files_modified: [src/pdf_accessibility/skills/validation/layout.py, src/pdf_accessibility/models/compliance.py]
autonomous: true
requirements: [OCR-02, OCR-03]

must_haves:
  truths:
    - "Logical reading order jumps are flagged during validation"
    - "Documents without headings are flagged as a warning"
    - "Compliance profiles correctly activate the new layout validation skills"
  artifacts:
    - path: "src/pdf_accessibility/skills/validation/layout.py"
      provides: "Layout-specific validation skills"
    - path: "src/pdf_accessibility/models/compliance.py"
      provides: "Updated profile definitions with layout validation"
  key_links:
    - from: "src/pdf_accessibility/models/compliance.py"
      to: "src/pdf_accessibility/skills/validation/layout.py"
      via: "skill_id in validation_skill_ids"
---

<objective>
Implement validation skills to verify logical reading order and document structure (headings, lists).

Purpose: Ensure the parsed and canonicalized document meets accessibility structural standards.
Output: New validation skills and updated compliance profiles.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/pdf_accessibility/skills/base.py
@src/pdf_accessibility/models/compliance.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Layout Validation Skills</name>
  <files>src/pdf_accessibility/skills/validation/layout.py</files>
  <behavior>
    - `LogicalReadingOrderSkill` (VALID-DOC-002): Flags large vertical jumps in reading order (e.g., bottom-to-top moves).
    - `SemanticStructureSkill` (VALID-DOC-003): Flags documents with no headings detected.
    - Skills should return `ValidationFinding` objects with appropriate severity (Warning/Error).
  </behavior>
  <action>
    - Create `src/pdf_accessibility/skills/validation/layout.py`.
    - Implement `LogicalReadingOrderSkill` using geometric analysis of sorted blocks.
    - Implement `SemanticStructureSkill` by checking for heading roles in `CanonicalDocument`.
  </action>
  <verify>
    <automated>pytest tests/test_validation_layout_skills.py</automated>
  </verify>
  <done>New layout validation skills implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Register Layout Skills in Compliance Profiles</name>
  <files>src/pdf_accessibility/models/compliance.py</files>
  <action>
    - Register `VALID-DOC-002` and `VALID-DOC-003` in the `PROFILE_DEFINITIONS` of `src/pdf_accessibility/models/compliance.py`.
    - Ensure all three profiles (A, B, C) include these layout checks.
  </action>
  <verify>
    <automated>pytest tests/test_compliance_profile_update.py</automated>
  </verify>
  <done>Compliance profiles now trigger layout and structure validation.</done>
</task>

</tasks>

<verification>
- Run validation on a document with known reading order issues and confirm findings are generated.
- Run validation on a heading-less document and confirm structural warning is emitted.
</verification>

<success_criteria>
- Validation findings include layout and structure reports.
- Compliance profile selection correctly influences validation output.
- Every layout finding includes page number and context.
</success_criteria>

<output>
After completion, create `.planning/phases/02-parsing-layout-foundation/02-03-SUMMARY.md`
</output>
