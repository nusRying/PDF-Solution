---
phase: 02-parsing-layout-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/pdf_accessibility/models/canonical.py, src/pdf_accessibility/models/documents.py, src/pdf_accessibility/services/pdf_parser.py]
autonomous: true
requirements: [CORE-01, CORE-02]

must_haves:
  truths:
    - "CanonicalDocument contains semantic roles for text blocks (e.g., Heading 1, List, Table)"
    - "ParserArtifact includes font size, name, and style flags for each block"
    - "pdf_parser.py extracts detailed layout data using PyMuPDF dict output"
  artifacts:
    - path: "src/pdf_accessibility/models/canonical.py"
      provides: "Updated CanonicalBlock with role and font info"
    - path: "src/pdf_accessibility/services/pdf_parser.py"
      provides: "Enhanced parser extracting layout cues"
  key_links:
    - from: "src/pdf_accessibility/services/pdf_parser.py"
      to: "ParserArtifact"
      via: "page.get_text('dict')"
---

<objective>
Update document models and the PDF parser to support semantic roles and detailed layout metadata.

Purpose: Provide the foundation for reading order reconstruction and structural analysis.
Output: Refined models and a layout-aware PDF parser.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@src/pdf_accessibility/models/canonical.py
@src/pdf_accessibility/models/documents.py
@src/pdf_accessibility/services/pdf_parser.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Models for Layout and Roles</name>
  <files>src/pdf_accessibility/models/canonical.py, src/pdf_accessibility/models/documents.py</files>
  <behavior>
    - Add `CanonicalRole` enum to `canonical.py`: [heading1-6, list, list_item, table, figure, caption, text, artifact]
    - Add `role: CanonicalRole` to `CanonicalBlock` (default `text`)
    - Add font metadata fields to `ParserTextBlock` and `CanonicalBlock`: `font_size` (float), `font_name` (str), `font_flags` (int)
  </behavior>
  <action>
    - Update `src/pdf_accessibility/models/canonical.py` with `CanonicalRole` enum and new `CanonicalBlock` fields.
    - Update `src/pdf_accessibility/models/documents.py` to include font metadata in `ParserTextBlock`.
  </action>
  <verify>
    <automated>pytest tests/test_models_layout.py</automated>
  </verify>
  <done>Models updated and validated with Pydantic serialization tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enhance PDF Parser for Layout Extraction</name>
  <files>src/pdf_accessibility/services/pdf_parser.py</files>
  <behavior>
    - Parser should use `page.get_text("dict")` to access span-level metadata.
    - Extract most frequent font size and name for each block.
    - Preserve font flags (bold, italic) from the spans.
    - Map these details into the updated `ParserTextBlock`.
  </behavior>
  <action>
    - Modify `parse_pdf` in `src/pdf_accessibility/services/pdf_parser.py`.
    - Replace `page.get_text("blocks")` with `page.get_text("dict")` logic.
    - Implement a helper to aggregate span data (font size/name) into the block level.
  </action>
  <verify>
    <automated>pytest tests/test_pdf_parser_enhanced.py</automated>
  </verify>
  <done>PDF parser correctly extracts font size/name/flags from sample PDFs.</done>
</task>

</tasks>

<verification>
- Run all tests to ensure model compatibility.
- Verify that `ParserArtifact` JSON output contains the new font and layout fields.
</verification>

<success_criteria>
- `ParserTextBlock` includes font and style metadata.
- `CanonicalBlock` supports semantic roles.
- All tests pass in the `revival` environment.
</success_criteria>

<output>
After completion, create `.planning/phases/02-parsing-layout-foundation/02-01-SUMMARY.md`
</output>
