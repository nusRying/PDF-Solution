---
phase: 02-parsing-layout-foundation
plan: 02
type: execute
wave: 2
depends_on: [02-parsing-layout-foundation-01]
files_modified: [src/pdf_accessibility/services/reading_order.py, src/pdf_accessibility/services/canonicalize.py]
autonomous: true
requirements: [CORE-03, OCR-01]

must_haves:
  truths:
    - "Blocks are correctly ordered for multi-column layouts"
    - "Semantic roles are automatically assigned based on font cues (size, weight)"
    - "Canonicalization process preserves and refines extracted layout data"
  artifacts:
    - path: "src/pdf_accessibility/services/reading_order.py"
      provides: "ReadingOrderEngine for sorting blocks"
    - path: "src/pdf_accessibility/services/canonicalize.py"
      provides: "Updated canonicalization logic"
  key_links:
    - from: "src/pdf_accessibility/services/canonicalize.py"
      to: "src/pdf_accessibility/services/reading_order.py"
      via: "sort_blocks_by_layout"
---

<objective>
Implement logical reading order reconstruction and update canonicalization to assign initial semantic roles from layout cues.

Purpose: Transform raw extraction data into a logically structured document model.
Output: Reading order engine and updated canonicalizer.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/pdf_accessibility/services/canonicalize.py
@src/pdf_accessibility/models/canonical.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ReadingOrderEngine</name>
  <files>src/pdf_accessibility/services/reading_order.py</files>
  <behavior>
    - Correctly handle single-column layouts (top-down, left-to-right).
    - Identify columns by significant vertical overlap and horizontal separation.
    - Sort within columns first, then by column X position.
    - Provide a robust sorting function that accepts a list of blocks and page dimensions.
  </behavior>
  <action>
    - Create `src/pdf_accessibility/services/reading_order.py`.
    - Implement a `ReadingOrderEngine` class with a `sort_blocks` method.
    - Use XY-based heuristics to handle columns.
  </action>
  <verify>
    <automated>pytest tests/test_reading_order.py</automated>
  </verify>
  <done>Reading order engine sorts blocks correctly for various layout types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Canonicalization and Role Assignment</name>
  <files>src/pdf_accessibility/services/canonicalize.py</files>
  <behavior>
    - Use `ReadingOrderEngine` to sort blocks during canonicalization.
    - Assign semantic roles (Heading 1-6) based on font size/boldness relative to page median.
    - Preserve the parser's font/layout metadata in the final `CanonicalDocument`.
  </behavior>
  <action>
    - Update `src/pdf_accessibility/services/canonicalize.py`.
    - Integrate `ReadingOrderEngine` into the block processing loop.
    - Implement role inference logic using font cues (e.g., `font_size > median_font_size` -> Heading).
  </action>
  <verify>
    <automated>pytest tests/test_canonicalization_enhanced.py</automated>
  </verify>
  <done>Canonical document features correct reading order and basic semantic roles.</done>
</task>

</tasks>

<verification>
- Process a multi-column PDF and verify that extracted text flows logically in the output.
- Check role assignments for large/bold text in sample documents.
</verification>

<success_criteria>
- Text blocks follow a natural reading flow.
- Headings are correctly identified from font properties.
- Full pipeline integration from parser -> reading order -> canonical model.
</success_criteria>

<output>
After completion, create `.planning/phases/02-parsing-layout-foundation/02-02-SUMMARY.md`
</output>
