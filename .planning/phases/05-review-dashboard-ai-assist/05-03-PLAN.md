---
phase: 05-review-dashboard-ai-assist
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified: [
  src/pdf_accessibility/api/routes/review.py,
  src/pdf_accessibility/main.py,
  tests/test_review_api.py
]
autonomous: true
requirements: [REVIEW-01, REVIEW-02, REVIEW-03]

must_haves:
  truths:
    - "GET /api/v1/documents/{document_id}/review returns pages/blocks flagged for review."
    - "POST /api/v1/documents/{document_id}/review/actions applies manual corrections."
    - "Manual actions trigger a document re-remediation to reflect changes."
  artifacts:
    - path: "src/pdf_accessibility/api/routes/review.py"
      provides: "FastAPI endpoints for review dashboard"
    - path: "tests/test_review_api.py"
      provides: "E2E tests for the review flow"
  key_links:
    - from: "src/pdf_accessibility/api/routes/review.py"
      to: "src/pdf_accessibility/services/review.py"
      via: "ReviewService function calls"
    - from: "src/pdf_accessibility/api/routes/review.py"
      to: "src/pdf_accessibility/services/remediation.py"
      via: "Re-running remediation pipeline"
---

<objective>
Expose the review functionality via REST API and ensure full integration with the remediation pipeline.

Purpose: Provide an interface for external dashboards to retrieve review items and submit manual corrections.
Output: Operational review API endpoints and integration tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pdf_accessibility/api/routes/documents.py
@src/pdf_accessibility/services/review.py
@src/pdf_accessibility/services/remediation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Review API Routes</name>
  <files>src/pdf_accessibility/api/routes/review.py</files>
  <action>
    Implement API endpoints:
    - `GET /api/v1/documents/{document_id}/review`:
      - Fetches the remediated canonical document.
      - Filters and returns pages/blocks where `needs_review=True` or AI confidence is low.
    - `POST /api/v1/documents/{document_id}/review/actions`:
      - Accepts a list of `ManualOverride` actions.
      - Calls `ReviewService.save_overrides`.
      - Triggers `run_remediation_pipeline` to update the document artifact.
  </action>
  <verify>
    <automated>pytest tests/test_review_api.py</automated>
  </verify>
  <done>Review endpoints implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Register Review Router</name>
  <files>src/pdf_accessibility/main.py</files>
  <action>
    - Import `review.router` from `src.pdf_accessibility.api.routes.review`.
    - Register it in the FastAPI app instance under `/api/v1`.
  </action>
  <verify>
    <automated>curl -f http://localhost:8000/api/v1/health</automated>
  </verify>
  <done>Review API routes registered.</done>
</task>

<task type="auto">
  <name>Task 3: Integration Test - End-to-End Review Flow</name>
  <files>tests/test_review_flow_e2e.py</files>
  <action>
    Create a test case that:
    1. Uploads a document with a figure and an ambiguous list item.
    2. Runs automated remediation (which flags items for review).
    3. Fetches items via `GET .../review`.
    4. Submits manual corrections (alt-text, role) via `POST .../review/actions`.
    5. Verifies that the final `remediated-canonical-output` contains the manual values.
  </action>
  <verify>
    <automated>pytest tests/test_review_flow_e2e.py</automated>
  </verify>
  <done>E2E review flow verified with tests.</done>
</task>

</tasks>

<verification>
Ensure that calling the POST review actions endpoint results in a document that passes validation with the manual corrections applied.
</verification>

<success_criteria>
- Review API correctly filters blocks needing attention.
- POST actions result in updated canonical artifacts on disk.
- E2E test passes in the revival environment.
</success_criteria>

<output>
After completion, create `.planning/phases/05-review-dashboard-ai-assist/05-03-SUMMARY.md`
</output>
