---
phase: 05-review-dashboard-ai-assist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  src/pdf_accessibility/services/ai_assist.py,
  src/pdf_accessibility/skills/remediation/ai_assist.py,
  src/pdf_accessibility/skills/registry.py
]
autonomous: true
requirements: [REMED-02]

must_haves:
  truths:
    - "AIAssistService provides a mock vision-based alt-text generator."
    - "AIAssistService provides a mock role disambiguation suggestor."
    - "AIAltTextSkill can apply generated alt-text to figure blocks."
    - "RoleDisambiguationSkill can suggest roles for low-confidence blocks."
  artifacts:
    - path: "src/pdf_accessibility/services/ai_assist.py"
      provides: "AI Assist Service with vision and role logic"
    - path: "src/pdf_accessibility/skills/remediation/ai_assist.py"
      provides: "AIAltTextSkill and RoleDisambiguationSkill implementations"
  key_links:
    - from: "src/pdf_accessibility/skills/remediation/ai_assist.py"
      to: "src/pdf_accessibility/services/ai_assist.py"
      via: "AIAssistService instance usage"
---

<objective>
Implement the AIAssistService and its corresponding remediation skills to provide AI-generated accessibility suggestions.

Purpose: Automate complex remediation tasks (alt-text, role disambiguation) using AI logic (initially mocked).
Output: A functional AIAssistService and skills integrated into the registry.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pdf_accessibility/skills/base.py
@src/pdf_accessibility/services/remediation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AIAssistService</name>
  <files>src/pdf_accessibility/services/ai_assist.py</files>
  <action>
    Create AIAssistService with the following methods:
    - `generate_alt_text(block: CanonicalBlock, context: str = "") -> tuple[str, float]`: Mocked vision logic. Returns (alt_text, confidence).
    - `disambiguate_role(block: CanonicalBlock) -> tuple[CanonicalRole, float]`: Mocked role logic (e.g., if block text starts with a bullet point but role is 'text', suggest 'list_item').
    Include a generic `Settings` dependency for future LLM integration.
  </action>
  <verify>
    <automated>pytest tests/test_ai_assist_service.py</automated>
  </verify>
  <done>AIAssistService implemented with mocked AI logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AI-based Remediation Skills</name>
  <files>src/pdf_accessibility/skills/remediation/ai_assist.py</files>
  <action>
    Implement two new remediation skills:
    1. `AIAltTextSkill` (ID: REMED-AI-001):
       - Iterates over 'figure' blocks without alt-text.
       - Calls `ai_assist_service.generate_alt_text`.
       - Applies the text and records a `RemediationAction`.
       - Flags page for review if confidence < 0.7.
    2. `RoleDisambiguationSkill` (ID: REMED-AI-002):
       - Iterates over blocks with low confidence or ambiguous patterns.
       - Calls `ai_assist_service.disambiguate_role`.
       - Applies the role and records a `RemediationAction`.
       - Flags page for review if confidence < 0.8.
  </action>
  <verify>
    <automated>pytest tests/test_ai_skills.py</automated>
  </verify>
  <done>AIAltTextSkill and RoleDisambiguationSkill implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Register AI Skills</name>
  <files>src/pdf_accessibility/skills/registry.py, src/pdf_accessibility/services/remediation.py</files>
  <action>
    - Import and register the new skills in `src/pdf_accessibility/skills/registry.py` (if applicable) or update the initialization in `src/pdf_accessibility/services/remediation.py`.
    - Ensure they are available in the Skill Registry.
  </action>
  <verify>
    <automated>pytest tests/test_skill_registry.py</automated>
  </verify>
  <done>Skills registered and accessible via the registry.</done>
</task>

</tasks>

<verification>
Check that the AIAssistService returns expected mock values and that the skills correctly apply those values to a CanonicalDocument.
</verification>

<success_criteria>
- AIAssistService methods return non-empty results.
- AIAltTextSkill populates alt_text field for figures.
- RoleDisambiguationSkill updates roles for blocks.
- Documentation for future LLM integration is present in AIAssistService.
</success_criteria>

<output>
After completion, create `.planning/phases/05-review-dashboard-ai-assist/05-01-SUMMARY.md`
</output>
