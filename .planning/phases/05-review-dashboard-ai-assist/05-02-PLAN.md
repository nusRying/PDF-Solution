---
phase: 05-review-dashboard-ai-assist
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [
  src/pdf_accessibility/models/review.py,
  src/pdf_accessibility/services/review.py,
  src/pdf_accessibility/services/file_store.py,
  src/pdf_accessibility/services/remediation.py,
  src/pdf_accessibility/core/settings.py
]
autonomous: true
requirements: [REVIEW-01, REVIEW-03]

must_haves:
  truths:
    - "ManualOverride model captures block-level corrections (role, alt_text)."
    - "ReviewService can persist manual overrides to the file store."
    - "ReviewService can retrieve existing overrides for a document."
    - "Remediation pipeline applies manual overrides at the final stage."
  artifacts:
    - path: "src/pdf_accessibility/models/review.py"
      provides: "ManualOverride and ReviewArtifact models"
    - path: "src/pdf_accessibility/services/review.py"
      provides: "ReviewService for managing human corrections"
  key_links:
    - from: "src/pdf_accessibility/services/remediation.py"
      to: "src/pdf_accessibility/services/review.py"
      via: "Applying overrides in run_remediation_pipeline"
---

<objective>
Implement the ReviewService and supporting models to handle human-in-the-loop corrections.

Purpose: Provide a mechanism to persist and apply manual overrides to the automated remediation results.
Output: A ReviewService that integrates with FileStore and the remediation pipeline.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pdf_accessibility/models/canonical.py
@src/pdf_accessibility/services/file_store.py
@src/pdf_accessibility/services/remediation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Review Models</name>
  <files>src/pdf_accessibility/models/review.py</files>
  <action>
    Create models for manual review:
    - `ManualOverride`: Contains `block_id`, `role` (optional), `alt_text` (optional), `is_artifact` (bool).
    - `ReviewArtifact`: Contains `document_id`, `generated_at`, and a list of `ManualOverride` objects.
  </action>
  <verify>
    <automated>pytest tests/test_review_models.py</automated>
  </verify>
  <done>Review models defined.</done>
</task>

<task type="auto">
  <name>Task 2: Update FileStore and Settings</name>
  <files>src/pdf_accessibility/core/settings.py, src/pdf_accessibility/services/file_store.py</files>
  <action>
    - Add `review_artifacts_dir` to `Settings`.
    - Update `FileStore` with:
      - `review_artifact_path(document_id: str) -> Path`
      - `save_review_artifact(artifact: ReviewArtifact) -> None`
      - `get_review_artifact(document_id: str) -> ReviewArtifact | None`
  </action>
  <verify>
    <automated>pytest tests/test_file_store_review.py</automated>
  </verify>
  <done>FileStore updated to support review artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ReviewService and Integrate with Remediation</name>
  <files>src/pdf_accessibility/services/review.py, src/pdf_accessibility/services/remediation.py</files>
  <action>
    - Create `ReviewService` with methods:
      - `save_overrides(document_id: str, overrides: list[ManualOverride])`: Saves/merges overrides.
      - `apply_overrides(document: CanonicalDocument) -> CanonicalDocument`: Updates blocks in the document based on stored overrides.
    - Update `run_remediation_pipeline` in `src/pdf_accessibility/services/remediation.py` to call `review_service.apply_overrides` after all skills have run but before final totals are calculated.
  </action>
  <verify>
    <automated>pytest tests/test_review_integration.py</automated>
  </verify>
  <done>ReviewService implemented and integrated into the remediation pipeline.</done>
</task>

</tasks>

<verification>
Verify that manual overrides correctly overwrite the results of automated skills in the final remediated canonical document.
</verification>

<success_criteria>
- Manual overrides are persisted to disk.
- Overrides are applied correctly to the block role and alt_text fields.
- Remediation pipeline reflects manual changes in its output.
</success_criteria>

<output>
After completion, create `.planning/phases/05-review-dashboard-ai-assist/05-02-SUMMARY.md`
</output>
